<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link rel="stylesheet" href="/supplierForm.css">
</head>
<body>
    <h1><%= title %></h1>

    <form action="/suppliers/edit/<%= supplier.supplier_id %>" method="POST">
        <div>
            <label for="name">Name:</label>
            <input 
                type="text" 
                id="name" 
                name="name" 
                value="<%= supplier.name %>" 
                required
            >
        </div>

        <div>
            <label for="email">Email:</label>
            <input 
                type="email" 
                id="email" 
                name="email" 
                value="<%= supplier.email %>" 
                required
            >
        </div>

        <div>
            <label for="phone">Phone:</label>
            <input 
                type="text" 
                id="phone" 
                name="phone" 
                value="<%= supplier.phone %>" 
                required
            >
        </div>

        <button type="submit">Save Changes</button>
        <a href="/suppliers/details/<%= supplier.supplier_id %>" class="btn">Back to Details</a>
    </form>

</body>
</html>

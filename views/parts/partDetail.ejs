<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= title %></title>
        <link rel="stylesheet" href="/supplierList.css">
    </head>
    <body>
        <h1><%= title %></h1>
        <table>
            <thead>
                <tr>
                    <th>Category_id</th>
                    <th>Name</th>
                    <th>Price</th>
                    <th>Quantity</th>
                    <th>Description</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><%= part.category_id %></td>
                    <td><%= part.name %></td>
                    <td>$<%= part.price %></td>
                    <td><%= part.quantity %></td>
                    <td><%= part.description %></td>
                    <td>
                        <section class="btn">
                            <a href="/parts/edit/<%= part.part_id %>">Edit</a>
                            <form action="/parts/delete/<%= part.part_id %>" method="POST">
                                <button type="submit" onclick="return confirm('Are you sure you want to delete this Item?');">Delete</button>
                            </form>
                            <a href="/categories/details/<%= part.category_id %>">Go Back</a>
                        </section>
                    </td>
                </tr>
            </tbody>
        </table>
    </body>
</html>